import 'package:flutter/material.dart';

class CustomizedBody extends StatelessWidget {
  const CustomizedBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}